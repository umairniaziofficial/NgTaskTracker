import { HighlighCompletedTodoDirective } from './highligh-completed-todo.directive';

describe('HighlighCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
