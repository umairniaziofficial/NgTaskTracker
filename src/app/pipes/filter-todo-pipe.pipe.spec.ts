import { FilterTodoPipePipe } from './filter-todo-pipe.pipe';

describe('FilterTodoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
